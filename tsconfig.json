{
  "// note": "Solution-style root config. `tsc -b` builds referenced packages in dependency order.",
  "files": [],
  "references": [
    { "path": "./packages/shared" },
    { "path": "./packages/server" }
  ]
}
